<div class="container">
  <h1>My tasks - List all tasks!</h1>
  <% @tasks.each do |task| %>
    <h2>Title: <%= link_to task.title, task_path(task) %></h2>
    <h2><%= link_to '🗑', task_path(task), class: 'btn btn-light', data: { turbo_method: :delete, turbo_confirm: "Delete #{task.title}?" } %></h2>
  <% end %>
  <%= link_to "Add a new task", new_task_path %>
</div>
